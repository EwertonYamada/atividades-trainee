package terminal.utils.enums;

public enum MenuOptions {
    SAQUE(1, "Saque"),
    DEPOSITO(2, "Deposito"),
    TRANSFERENCIA(3, "Transferência"),
    VER_SALDO(4, "Ver saldo"),
    SAIR(0, "Sair");

    MenuOptions(int i, String transferência) {}
}
